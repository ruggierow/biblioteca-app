{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões padrão para a janela principal",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
